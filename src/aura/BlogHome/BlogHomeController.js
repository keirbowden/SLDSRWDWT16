({
	loadInitialPosts : function(component, event, helper) {
		helper.loadInitialPosts(component, event);
	},
    search : function(component, event, helper) {
        helper.search(component, event);
    },
    resetSearch : function(component, event, helper) {
        helper.resetSearch(component, event);
    },
    comment : function(component, event, helper) {
        helper.comment(component, event);
    },
    navigateToLinks : function(component, event, helper) {
        helper.navigateToLinks(component, event);
    },
    createPosts : function(component, event, helper) {
        helper.createPosts(component, event);
    }
})